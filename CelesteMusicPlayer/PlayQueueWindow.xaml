<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CelesteMusicPlayer.PlayQueueWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CelesteMusicPlayer"
    Title="播放队列">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid
            x:Name="AppTitleBar"
            Grid.Row="0"
            Height="48"
            Padding="12,0,0,0"
            Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="138" />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="10"
                VerticalAlignment="Center"
                Padding="4,0,8,0">
                <FontIcon Glyph="&#xE139;" FontSize="16" Opacity="0.95" />
                <TextBlock VerticalAlignment="Center" FontSize="13" Opacity="0.9" Text="播放队列" />
            </StackPanel>
            <TextBlock
                x:Name="QueueCountText"
                Grid.Column="1"
                VerticalAlignment="Center"
                FontSize="12"
                Opacity="0.6"
                Margin="0,0,12,0"
                HorizontalAlignment="Right" />
        </Grid>

        <!-- 说明条 -->
        <Grid Grid.Row="1" Padding="12,0,12,8">
            <TextBlock
                x:Name="QueueHintText"
                FontSize="12"
                Opacity="0.7"
                Text="接下来的待播曲目（当前曲之后）。双击跳转，可拖拽重排。" />
        </Grid>

        <!-- 队列列表 -->
        <ListView
            x:Name="QueueListView"
            Grid.Row="2"
            SelectionMode="Single"
            DoubleTapped="QueueListView_DoubleTapped"
            DragItemsCompleted="QueueListView_DragItemsCompleted"
            CanReorderItems="True"
            AllowDrop="True"
            Padding="2,0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Padding" Value="8,2" />
                    <Setter Property="MinHeight" Value="36" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PlaylistItem">
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" VerticalAlignment="Center" Opacity="0.5" FontSize="11"
                                   Text="{x:Bind TrackText}" />
                        <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Artist}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <TextBlock Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center"
                                   Opacity="0.6" Text="{x:Bind DurationText}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.DataContext />
        </ListView>

        <!-- 底部操作 -->
        <Grid Grid.Row="3" Padding="12,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="BottomHintText"
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="12"
                Opacity="0.6" />
            <Button
                x:Name="ClearQueueButton"
                Grid.Column="1"
                Content="清空队列"
                Click="ClearQueueButton_Click"
                Padding="12,4" />
        </Grid>
    </Grid>
</Window>
