<Window
    x:Class="CelesteMusicPlayer.ArtistAvatarSearchWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CelesteMusicPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 自定义标题栏（与在线搜索窗口一致） -->
        <Grid
            x:Name="AppTitleBar"
            Height="48"
            Padding="16,0,0,0"
            Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="138" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE77B;" FontSize="16" />
                <TextBlock Text="搜索网络头像" FontSize="14" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Padding="14" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 顶部：搜索卡片 -->
            <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefault}"
                    CornerRadius="10" Padding="10">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="SourceCombo" Width="150" SelectionChanged="SourceCombo_SelectionChanged" />
                    <TextBox x:Name="SearchBox" Grid.Column="1" PlaceholderText="输入艺术家名…"
                             KeyDown="SearchBox_KeyDown" VerticalContentAlignment="Center" />
                    <Button x:Name="SearchButton" Grid.Column="2" Content="搜索"
                            Style="{StaticResource AccentButtonStyle}" Click="SearchButton_Click" />
                </Grid>
            </Border>

            <TextBlock x:Name="StatusText" Grid.Row="1" FontSize="12" Opacity="0.7"
                       TextWrapping="WrapWholeWords" />

            <!-- 主体：头像候选网格 -->
            <Grid Grid.Row="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <GridView x:Name="ResultGrid" SelectionMode="Single"
                              ItemClick="ResultGrid_ItemClick" IsItemClickEnabled="True">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="local:ArtistAvatarSearchItem">
                                <StackPanel Width="160" Spacing="6">
                                    <Border Width="160" Height="160" CornerRadius="10"
                                            Background="{ThemeResource SubtleFillColorSecondary}"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                        <Image Source="{x:Bind Cover}" Stretch="UniformToFill" />
                                    </Border>
                                    <TextBlock Text="{x:Bind Name}" FontSize="12" HorizontalAlignment="Center"
                                               TextTrimming="CharacterEllipsis" MaxLines="1" />
                                    <TextBlock Text="{x:Bind Platform}" FontSize="11" Opacity="0.7"
                                               HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
