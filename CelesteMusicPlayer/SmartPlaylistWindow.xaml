<Window
    x:Class="CelesteMusicPlayer.SmartPlaylistWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CelesteMusicPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="智能播放列表"
    Width="680" Height="540">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" FontSize="13">
            智能播放列表：用规则自动生成，每次「播放」都按当前曲库实时查询（随库更新）。
            支持评分 / 流派 / 艺术家 / 年代 / 最常播放 / 最近加入。
        </TextBlock>

        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 左侧：已保存列表 -->
            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="已保存" FontWeight="SemiBold" />
                <ListView
                    x:Name="PlaylistList"
                    Height="360"
                    SelectionChanged="PlaylistList_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:SmartPlaylistRow">
                            <StackPanel Spacing="2" Padding="4">
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind Rule}" FontSize="11" Opacity="0.7" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="PlayButton" Content="播放" Click="PlayButton_Click" IsEnabled="False" />
                    <Button x:Name="AddButton" Content="添加到队列" Click="AddButton_Click" IsEnabled="False" />
                    <Button x:Name="DeleteButton" Content="删除" Click="DeleteButton_Click" IsEnabled="False" />
                </StackPanel>
            </StackPanel>

            <!-- 右侧：编辑器 -->
            <StackPanel Grid.Column="1" Spacing="10">
                <TextBlock Text="编辑规则" FontWeight="SemiBold" />
                <TextBox x:Name="NameBox" Header="名称" PlaceholderText="例如：我的 4 星以上" />
                <ComboBox x:Name="KindCombo" Header="规则类型" SelectionChanged="KindCombo_SelectionChanged">
                    <ComboBoxItem Content="评分 ≥ N 星" Tag="Rating" />
                    <ComboBoxItem Content="按流派" Tag="Genre" />
                    <ComboBoxItem Content="按艺术家" Tag="Artist" />
                    <ComboBoxItem Content="按年代" Tag="Decade" />
                    <ComboBoxItem Content="最常播放" Tag="MostPlayed" />
                    <ComboBoxItem Content="最近加入" Tag="RecentlyAdded" />
                </ComboBox>
                <ComboBox x:Name="ArgCombo" Header="参数" Visibility="Collapsed" />
                <TextBox x:Name="ArgText" Header="参数（星数 1-5）" Visibility="Collapsed" PlaceholderText="4" />
                <NumberBox x:Name="LimitBox" Header="数量上限" Value="100" Minimum="1" Maximum="2000" SpinButtonPlacementMode="Inline" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="SaveButton" Content="保存" Click="SaveButton_Click" />
                    <Button x:Name="NewButton" Content="新建" Click="NewButton_Click" />
                </StackPanel>
                <TextBlock x:Name="StatusText" FontSize="11" Opacity="0.75" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
