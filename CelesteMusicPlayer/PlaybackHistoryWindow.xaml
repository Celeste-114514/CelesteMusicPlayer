<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CelesteMusicPlayer.PlaybackHistoryWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CelesteMusicPlayer"
    Title="播放历史">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid
            x:Name="AppTitleBar"
            Grid.Row="0"
            Height="48"
            Padding="12,0,0,0"
            Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Text="播放历史"
                FontSize="15"
                FontWeight="SemiBold"
                VerticalAlignment="Center" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button x:Name="RefreshButton" Content="刷新" Click="RefreshButton_Click" />
                <Button x:Name="ClearButton" Content="清空" Click="ClearButton_Click" />
            </StackPanel>
        </Grid>

        <TextBlock
            x:Name="HistoryHint"
            Grid.Row="1"
            Margin="16,4,16,8"
            FontSize="12"
            Opacity="0.65"
            TextWrapping="WrapWholeWords"
            Text="最近播放的曲目（双击可重新播放）。切换歌曲、播完或停止时记录。" />

        <ListView
            x:Name="HistoryListView"
            Grid.Row="2"
            Margin="12,0,12,0"
            DoubleTapped="HistoryListView_DoubleTapped"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="4,6" Spacing="2">
                        <TextBlock
                            Text="{Binding DisplayTitle}"
                            FontSize="14"
                            FontWeight="SemiBold"
                            TextTrimming="CharacterEllipsis" />
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{Binding DisplayTime}" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="{Binding DisplayDuration}" FontSize="11" Opacity="0.7" />
                            <TextBlock Text="{Binding StatusText}" FontSize="11" Opacity="0.55" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock
            x:Name="HistoryEmptyText"
            Grid.Row="2"
            Text="还没有播放记录。去播一首歌吧。"
            FontSize="13"
            Opacity="0.55"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="Collapsed" />

        <TextBlock
            Grid.Row="3"
            Margin="12,8"
            FontSize="11"
            Opacity="0.45"
            Text="历史记录保存在本地 SQLite（library.db），可随时清空。" />
    </Grid>
</Window>
