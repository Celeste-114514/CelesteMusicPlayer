# ===== .NET / Visual Studio =====
bin/
obj/
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# ===== 构建与发布输出 =====
AppPackages/
BundleArtifacts/
*.appx
*.msix
*.msixbundle
*.appxbundle
*.cer
publish/
DesktopRelease/
发布/

# ===== 运行时生成 =====
*.log
*.partial.wav

# ===== IDE / 系统 =====
.vscode/
.idea/
Thumbs.db
Desktop.ini

# ===== 用户本地配置(不入库) =====
local.settings.json
*.local.json

# Local dev packaging signing key (private key - never commit)
*.pfx
# 本地签名密码（从 csproj 外置，避免密码随代码入库）
CelesteMusicPlayer/CelesteMusicPlayer.signing.props

# ===== 流媒体插件预编译二进制与发布包（不入库，作 release 附件） =====
streaming-plugin/streamingserver

# 2026-08-28: git 对象库损坏后保留的备份目录（确认无需恢复后可手动删除）
.git.damaged-*/

# ===== 原生（ECHO 核心）编译中间产物（celeste_audio_core.dll 本体要入库随包分发）=====
native/echo-core/build/*.obj
native/echo-core/build/*.exp
native/echo-core/build/*.lib

# ===== 原生（自研 celeste-core）编译中间产物（celeste_core.dll 本体要入库随包分发）=====
# 注意：build/libs/ 下的 .lib 是无空格拷贝，必须入库，不能被上面规则误伤
# （单层 *.lib 只匹配 build/ 直属，不递归子目录）
native/celeste-core/build/*.obj
native/celeste-core/build/*.exp
native/celeste-core/build/*.lib
native/celeste-core/build/build_*.log
